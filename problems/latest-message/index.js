import R from 'ramda';
const { chain, compose, curry, curryN, partial, partialRight, pipe } = R;
import Maybe, { Just, Nothing } from '../../solutions/containers/Maybe.js';

// find :: (a -> Boolean) -> [a] -> Maybe a
export const find;

// propEq :: String -> Any -> {String: Any} -> Boolean
export const propEq;

// getLatestMessage :: String -> [a] -> Maybe a
export default getLatestMessage;
