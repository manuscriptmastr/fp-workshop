import R from 'ramda';
const { compose, chain, curry, curryN, partial, partialRight, map, pipe } = R;
import Maybe, { Just, Nothing } from '../../solutions/containers/Maybe';

// data Symbol = String | Int
// prop :: Symbol -> {Symbol: a} -> Maybe a
export const prop;

// path :: [Symbol] -> {Symbol: a} -> Maybe a
export default path;
